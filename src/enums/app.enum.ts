// 路由枚举
export enum RouterEnum {
  LOGIN = "/pages/login/index",
  HOME = "/pages/home/index",
}
// 存储key
export enum StorageEnum {
  TOKEN = "token",
  VIEW_VERSION = "viewVersion",
  POSITION = "position",
}
// oss 路径
export enum OssPathEnum {
  // 默认
  DEFAULT = "weapp/default/",
}
